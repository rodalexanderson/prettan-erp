import React from 'react'

const Admin = ({role}) => {
    
      return (
        <div className="w-full min-h-36 mt-8">
          {/* Contenido de la página para el rol de admin */}
        </div>
      );
    };

export default Admin